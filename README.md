# Nxance Early Access

Next.js 16 + Tailwind CSS + Supabase app for the Nxance early access page.

## Run locally

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

Environment variables live in `.env.local` (see `.env.example`).
